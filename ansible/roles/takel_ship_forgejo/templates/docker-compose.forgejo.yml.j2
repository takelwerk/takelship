---
networks:
  {{ takel_ship_forgejo_service['name'] }}:
    external: false

services:
  {{ takel_ship_forgejo_service['name'] }}:
    image: {{ takel_ship_forgejo_image['alias'] }}:{{ takel_ship_forgejo_image['tag'] }}
    container_name: {{ takel_ship_forgejo_service['name'] }}
    environment:
      - USER_UID={{ takel_ship_forgejo_uid }}
      - USER_GID={{ takel_ship_forgejo_gid }}
    restart: {{ takel_ship_forgejo_restart }}
    networks:
      - {{ takel_ship_forgejo_service['name'] }}
    volumes:
      - ./{{ takel_ship_services_include }}data:/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "{{ takel_ship_forgejo_http_port }}:3000"
      - "{{ takel_ship_forgejo_ssh_port }}:22"
