{{ takel_ship_scripts_shebang }}

mount / --make-rshare
cd /home/podman
if [ $# -eq 0 ]; then
  CONTAINER_COMMAND="{{ takel_ship_forgejo_bin_container_command }} --help"
else
  CONTAINER_COMMAND="{{ takel_ship_forgejo_bin_container_command }} $@"
fi
{{ takel_ship_scripts_script_cmd['name'] }} "$CONTAINER_COMMAND"
