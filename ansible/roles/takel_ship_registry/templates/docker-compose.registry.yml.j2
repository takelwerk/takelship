---
networks:
  {{ takel_ship_registry_service['name'] }}:
    external: false

services:
  {{ takel_ship_registry_service['name'] }}:
    image: {{ takel_ship_registry_image['name'] }}:{{ takel_ship_registry_image['tag'] }}
    container_name: {{ takel_ship_registry_service['name'] }}
    restart: {{ takel_ship_registry_restart }}
    networks:
      - {{ takel_ship_registry_service['name'] }}
    volumes:
      - ./{{ takel_ship_services_include }}data:/var/lib/registry
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    ports:
      - "{{ takel_ship_registry_http_port }}:5000"
