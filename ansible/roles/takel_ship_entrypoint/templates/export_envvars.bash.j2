{{ takel_ship_scripts_shebang }}

ETC_ENVIRONMENT={{ takel_ship_entrypoint_etc_environment }}

for ENVVAR in ${1}; do
  # if ENVVAR starts with TAKELSHIP_ append it to ETC_ENVIRONMENT
  [[ "$ENVVAR" == TAKELSHIP_* ]] && echo $ENVVAR >> $ETC_ENVIRONMENT
done
